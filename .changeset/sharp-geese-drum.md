---
"@tenex-eng/salt": patch
---

Fix packaged CSS mode coverage so root dark mode and elevated surface scopes re-declare the Tailwind color aliases used by Salt utilities, and include the heading font utility in standalone CSS output.
